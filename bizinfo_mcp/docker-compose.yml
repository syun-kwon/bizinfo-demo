services:
  bizinfo-mcp:
    build: .
    ports:
      - "8000:8000"
    environment:
      - BIZINFO_API_KEY=${BIZINFO_API_KEY}
      - MCP_TRANSPORT=sse
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
    restart: unless-stopped
